namespace Integraph.Models.IfcBase.Models
{
    public class IfcDerivedMeasureValue : Select
    {
        public IfcDerivedMeasureValue(IfcAbsorbedDoseMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcAccelerationMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcAngularVelocityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcAreaDensityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcCompoundPlaneAngleMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcCurvatureMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcDoseEquivalentMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcDynamicViscosityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcElectricCapacitanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcElectricChargeMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcElectricConductanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcElectricResistanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcElectricVoltageMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcEnergyMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcForceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcFrequencyMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcHeatFluxDensityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcHeatingValueMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcIlluminanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcInductanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcIntegerCountRateMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcIonConcentrationMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcIsothermalMoistureCapacityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcKinematicViscosityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLinearForceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLinearMomentMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLinearStiffnessMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLinearVelocityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLuminousFluxMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcLuminousIntensityDistributionMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMagneticFluxDensityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMagneticFluxMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMassDensityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMassFlowRateMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMassPerLengthMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcModulusOfElasticityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcModulusOfLinearSubgradeReactionMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcModulusOfRotationalSubgradeReactionMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcModulusOfSubgradeReactionMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMoistureDiffusivityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMolecularWeightMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMomentOfInertiaMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcMonetaryMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcPhMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcPlanarForceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcPowerMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcPressureMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcRadioActivityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcRotationalFrequencyMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcRotationalMassMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcRotationalStiffnessMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSectionModulusMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSectionalAreaIntegralMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcShearModulusMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSoundPowerLevelMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSoundPowerMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSoundPressureLevelMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSoundPressureMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcSpecificHeatCapacityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcTemperatureGradientMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcTemperatureRateOfChangeMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcThermalAdmittanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcThermalConductivityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcThermalExpansionCoefficientMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcThermalResistanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcThermalTransmittanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcTorqueMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcVaporPermeabilityMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcVolumetricFlowRateMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcWarpingConstantMeasure choice)
        {
            _choice = choice;
        }

        public IfcDerivedMeasureValue(IfcWarpingMomentMeasure choice)
        {
            _choice = choice;
        }
    }
}