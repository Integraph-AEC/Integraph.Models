namespace Integraph.Models.IfcBase.Models
{
    public class IfcMeasureValue : Select
    {
        public IfcMeasureValue(IfcAmountOfSubstanceMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcAreaMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcComplexNumber choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcContextDependentMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcCountMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcDescriptiveMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcElectricCurrentMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcLengthMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcLuminousIntensityMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcMassMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcNonNegativeLengthMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcNormalisedRatioMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcNumericMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcParameterValue choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcPlaneAngleMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcPositiveLengthMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcPositivePlaneAngleMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcPositiveRatioMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcRatioMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcSolidAngleMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcThermodynamicTemperatureMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcTimeMeasure choice)
        {
            _choice = choice;
        }

        public IfcMeasureValue(IfcVolumeMeasure choice)
        {
            _choice = choice;
        }
    }
}